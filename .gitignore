node_modules/
dist/
*.log
.DS_Store
.env
.env.local
state/

# Local-only design notes: kept on disk, never committed.
docs/
spec.md

# Your own dev host config; start from scripts/dev.example.json.
scripts/dev.json

# wrangler dev state (local D1, build cache).
.wrangler/
