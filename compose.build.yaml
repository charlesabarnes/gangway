# Build gangway from this checkout instead of pulling the published image:
#
#   docker compose -f compose.yaml -f compose.build.yaml up -d --build
#
# The local image is tagged gangway:<tag>, so it never shadows the registry's.
services:
  gangway:
    image: gangway:${GANGWAY_TAG:-latest}
    build: .
